Ext.onReady(function() {
    /**
     * パネル生成
     */
    var p = Ext.create('Ext.panel.Panel', {
        title: 'Minify', // タイトル
        width: 500, // 幅
        height: 300, // 高さ
        renderTo: Ext.getBody() // レンダリング先
    });
});
